:javascript
  $(function() {
    window.router = new AsqUs.Routers.RepsRouter({rep: #{@rep.to_json.html_safe}});
    Backbone.history.start();
  });
  window.setTimeout (function() {
    navItem.style.WebkitTransition = '-webkit-transform 300ms ease-in-out';
    navItem.style.WebkitTransform = 'translate3d(0, 0, 0) rotate(5deg)';
  }, 0);
    

= link_to image_tag( '/logo.jpg'), 'logo.jpg' 



#rep_wrapper
  = render :partial => 'reps/nav'
  #content.rightRounded

